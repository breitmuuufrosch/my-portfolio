export interface Trade {
  name: string,
  symbol: string,
  quoteType: string,
  currency: string,
  entryPrice: number,
  entryPriceAll: number,
  number: number,
  exitPrice: number,
  lastPrice: number,
}
